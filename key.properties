here```yaml
name: Build OWA Médical APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Decrypt and setup keystore
        env:
          KEYSTORE_BASE64: secrets.OWA_KEYSTORE_BASE64 
        run: |
          echo "KEYSTORE_BASE64" | base64 -d > android/app/owa-keystore.jks

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: owa-medical-apk
          path: build/app/outputs/flutter-apk/app-release.apk
